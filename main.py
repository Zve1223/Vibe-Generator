from aggregators.pipeline_aggregator import *

if __name__ == '__main__':
    pipeline(
        # specify_task,
        # rewrite_task_for_ai,
        create_project_tree,
        write_files_instructions,
    )
